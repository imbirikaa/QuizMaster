buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.2.2' // Ensure it's the correct AGP version
        classpath 'org.jetbrains.kotlin:kotlin-gradle-plugin:1.9.0' // Use the latest compatible Kotlin version
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
